{
  "type": "stdio",
  "command": "uv",
  "args": [
    "--directory",
    "C:\\Users\\YOUR-USERNAME\\Documents\\GitHub\\EnergyMe-Home\\mcp-server",
    "run",
    "server.py"
  ],
  "env": {
    "ENERGYME_BASE_URL": "${input:energyme_base_url}",
    "ENERGYME_USERNAME": "${input:energyme_username}",
    "ENERGYME_PASSWORD": "${input:energyme_password}"
  }
}